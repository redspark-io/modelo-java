machine:
  java:
    version: oraclejdk8
  #services:
    #- docker

dependencies:
  override:
    #- docker info
    - mvn dependency:resolve -f server/pom.xml

general:
  artifacts:
    # publish do core
    - "server/target/modelo.jar"

test:
  override:
    # build
    - mvn install -f server/pom.xml org.jacoco:jacoco-maven-plugin:report coveralls:report -DrepoToken=jsF9GATsd25lOgf6zZUsrNExlpjWdVagd

  post:
    # testes report
    - mkdir -p $CIRCLE_TEST_REPORTS/junit/
    - find . -type f -regex ".*/target/surefire-reports/.*xml" -exec cp {} $CIRCLE_TEST_REPORTS/junit/ \;
